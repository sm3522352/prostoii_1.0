/**
 * PostCSS configuration.
 *
 * Tailwind is configured as a PostCSS plugin. Additional plugins
 * can be added here if needed (e.g. autoprefixer is included by
 * default in Tailwind’s preset).
 */
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {}
  }
};